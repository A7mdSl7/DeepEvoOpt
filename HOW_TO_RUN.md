# 🚀 كيفية تشغيل DeepEvoOpt Dashboard

## خطوات التشغيل السريع

### 1. التأكد من تثبيت المتطلبات

افتح Terminal/PowerShell في مجلد المشروع وتأكد من تثبيت جميع المكتبات:

```bash
pip install -r requirements.txt
```

### 2. تشغيل التطبيق

#### الطريقة الأولى: استخدام Streamlit مباشرة

```bash
streamlit run app_streamlit.py
```

#### الطريقة الثانية: استخدام Python

```bash
python -m streamlit run app_streamlit.py
```

### 3. فتح المتصفح

بعد تشغيل الأمر، سيفتح المتصفح تلقائياً على العنوان:

```
http://localhost:8501
```

إذا لم يفتح تلقائياً، افتح المتصفح يدوياً واذهب إلى:

```
http://localhost:8501
```

---

## 📋 ما الذي ستراه؟

1. **لوحة التحكم الرئيسية** - صفحة Streamlit مع:

   - قائمة جانبية (Sidebar) لاختيار النماذج والخوارزميات
   - تبويبات مختلفة لعرض النتائج

2. **التبويبات المتاحة**:
   - 📊 Summary & Convergence - ملخص ومخططات التقارب
   - 📈 Distribution - توزيع النتائج
   - ⚙️ Best Parameters - أفضل المعاملات الفائقة
   - 🧪 Sample Evaluation - تقييم النماذج
   - 🚀 Run Experiments - تشغيل تجارب جديدة

---

## ⚠️ ملاحظات مهمة

1. **البيانات**: تأكد من وجود ملفات النتائج في `results/logs/` لعرض البيانات

   - إذا لم تكن موجودة، يمكنك تشغيل تجربة أولاً من التبويب "Run Experiments"

2. **إيقاف التطبيق**: اضغط `Ctrl + C` في Terminal لإيقاف التطبيق

3. **المشاكل الشائعة**:
   - إذا ظهرت رسالة "Port already in use"، استخدم:
     ```bash
     streamlit run app_streamlit.py --server.port 8502
     ```

---

## 🎯 مثال على الاستخدام

1. شغل التطبيق: `streamlit run app_streamlit.py`
2. من القائمة الجانبية، اختر:
   - Model: CNN أو MLP
   - Optimizers: اختر واحد أو أكثر (مثل GA, PSO)
3. شاهد النتائج في التبويبات المختلفة

---

## 💡 نصائح

- استخدم `Ctrl + R` في المتصفح لتحديث الصفحة
- يمكنك تشغيل تجارب جديدة مباشرة من التطبيق
- النتائج تُحفظ تلقائياً في `results/logs/`
