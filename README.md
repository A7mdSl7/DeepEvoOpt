# 🧬 DeepEvoOpt - نظام تحسين المعاملات الفائقة للتعلم العميق

## 📋 نظرة عامة

**DeepEvoOpt** هو نظام متقدم لتحسين المعاملات الفائقة (Hyperparameter Optimization) لنماذج التعلم العميق باستخدام خوارزميات التحسين التطورية (Meta-heuristic Optimization Algorithms). المشروع يهدف إلى إيجاد أفضل المعاملات الفائقة لنماذج CNN و MLP على مجموعة بيانات Fashion-MNIST باستخدام 9 خوارزميات تحسين مختلفة.

### الهدف من المشروع

المشروع يطبق خوارزميات التحسين التطورية لتحسين أداء نماذج التعلم العميق من خلال:
- البحث التلقائي عن أفضل المعاملات الفائقة (معدل التعلم، حجم الدفعة، معدل الإسقاط، وغيرها)
- مقارنة أداء خوارزميات التحسين المختلفة
- توفير واجهة تفاعلية لتصور النتائج ومقارنة الأداء

---

## 🏗️ بنية المشروع

```
DeepEvoOpt/
├── data/                      # مجلد مجموعة البيانات
│   └── FashionMNIST/         # بيانات Fashion-MNIST
│       └── raw/              # الملفات الخام للبيانات
│
├── notebooks/                 # دفاتر Jupyter للتجارب
│   ├── Experiments.ipynb     # دفتر التجارب الرئيسي
│   └── results/              # نتائج التجارب
│       └── logs/             # ملفات السجلات والنتائج
│
├── results/                   # النتائج والسجلات
│   └── logs/                 # ملفات JSON و CSV للنتائج
│
├── src/                       # الكود المصدري الرئيسي
│   ├── models/               # نماذج التعلم العميق
│   │   ├── cnn_model.py     # نموذج CNN (الشبكة العصبية الالتفافية)
│   │   └── nn_model.py      # نموذج MLP (الشبكة العصبية متعددة الطبقات)
│   │
│   ├── optimizers/           # خوارزميات التحسين التطورية
│   │   ├── ga.py            # خوارزمية الجينات (Genetic Algorithm)
│   │   ├── pso.py           # تحسين سرب الجسيمات (Particle Swarm Optimization)
│   │   ├── gwo.py           # محسن الذئاب الرمادية (Grey Wolf Optimizer)
│   │   ├── aco.py           # تحسين مستعمرة النمل (Ant Colony Optimization)
│   │   ├── firefly.py       # خوارزمية اليراعة (Firefly Algorithm)
│   │   ├── abc.py           # مستعمرة النحل الاصطناعية (Artificial Bee Colony)
│   │   ├── obc_woa.py       # خوارزمية الحوت المعاكسة الفوضوية (OBC-WOA)
│   │   ├── fcr.py           # إعادة التركيب المتمركزة على اللياقة (FCR)
│   │   └── fcgwo.py         # محسن الذئاب الرمادية بالتحكم الضبابي (FCGWO)
│   │
│   ├── utils/                # الوظائف المساعدة
│   │   ├── data_loader.py   # تحميل ومعالجة البيانات
│   │   ├── objective_fn.py  # دالة الهدف للتحسين
│   │   ├── helpers.py       # وظائف مساعدة عامة
│   │   └── check_data.py    # التحقق من البيانات
│   │
│   ├── gui/                  # واجهة المستخدم الرسومية (Streamlit)
│   │   ├── ui.py            # عناصر واجهة المستخدم
│   │   ├── plots.py         # الرسوم البيانية والتخطيطات
│   │   ├── data.py          # تحميل البيانات للواجهة
│   │   ├── run_control.py   # التحكم في تشغيل التجارب
│   │   └── styles.css       # ملفات التنسيق
│   │
│   ├── train.py             # سكريبت تدريب النموذج النهائي
│   ├── run_experiments.py   # سكريبت تشغيل التجارب الرئيسي
│   └── evaluate.py          # تقييم النماذج
│
├── app_streamlit.py          # تطبيق Streamlit الرئيسي
├── requirements.txt          # المتطلبات والاعتمادات
├── Dockerfile               # ملف Docker للنشر
├── README.md                # هذا الملف
└── report.pdf               # تقرير المشروع
```

---

## 🚀 الميزات الرئيسية

### 1. **خوارزميات التحسين التطورية (9 خوارزميات)**

المشروع يطبق 9 خوارزميات تحسين مختلفة:

1. **GA (Genetic Algorithm)** - خوارزمية الجينات
   - تستخدم التكاثر والطفرة والانتقاء الطبيعي
   - مناسبة للبحث في مساحات كبيرة

2. **PSO (Particle Swarm Optimization)** - تحسين سرب الجسيمات
   - تحاكي سلوك الطيور في السرب
   - سريعة في التقارب

3. **GWO (Grey Wolf Optimizer)** - محسن الذئاب الرمادية
   - تحاكي هرمية الصيد عند الذئاب
   - توازن جيد بين الاستكشاف والاستغلال

4. **ACO (Ant Colony Optimization)** - تحسين مستعمرة النمل
   - تحاكي سلوك النمل في البحث عن الطعام
   - فعالة في المساحات المنفصلة

5. **Firefly Algorithm** - خوارزمية اليراعة
   - تحاكي سلوك اليراعات في الجذب
   - جيدة للبحث المحلي

6. **ABC (Artificial Bee Colony)** - مستعمرة النحل الاصطناعية
   - تحاكي سلوك النحل في البحث عن الرحيق
   - توازن بين الاستكشاف والاستغلال

7. **OBC-WOA (Opposition-based Chaotic Whale Optimization)** - خوارزمية الحوت المعاكسة الفوضوية
   - تحسين متقدم لخوارزمية الحوت
   - استخدام الفوضى والمعاكسة لتحسين الأداء

8. **FCR (Fitness-Centered Recombination)** - إعادة التركيب المتمركزة على اللياقة
   - تركز على إعادة التركيب بناءً على اللياقة
   - تحسين معدل التقارب

9. **FCGWO (Fuzzy-Controlled GWO)** - محسن الذئاب الرمادية بالتحكم الضبابي
   - استخدام المنطق الضبابي لتحسين GWO
   - تحكم أفضل في المعاملات

### 2. **نماذج التعلم العميق**

#### **CNN (Convolutional Neural Network)**
- طبقتان التلافيتان (Convolutional Layers)
- طبقة MaxPooling
- طبقة Dropout لمنع الإفراط في التجهيز
- طبقات Fully Connected
- المعاملات القابلة للتحسين:
  - عدد قنوات الإخراج للطبقة الالتفافية الأولى (`conv1_out`)
  - حجم النواة الأولى (`kernel1`)
  - عدد قنوات الإخراج للطبقة الالتفافية الثانية (`conv2_out`)
  - حجم النواة الثانية (`kernel2`)
  - حجم الطبقة المتصلة بالكامل (`fc1_out`)
  - معدل الإسقاط (`dropout`)

#### **MLP (Multi-Layer Perceptron)**
- طبقتان مخفيتان (Hidden Layers)
- طبقة Dropout
- المعاملات القابلة للتحسين:
  - حجم الطبقة المخفية الأولى (`hidden1`)
  - حجم الطبقة المخفية الثانية (`hidden2`)
  - معدل الإسقاط (`dropout`)

### 3. **المعاملات الفائقة المشتركة**

جميع النماذج تشترك في المعاملات التالية:
- **معدل التعلم (Learning Rate)**: من 0.0001 إلى 0.01
- **حجم الدفعة (Batch Size)**: 32، 64، أو 128
- **معدل الإسقاط (Dropout Rate)**: من 0.1 إلى 0.5
- **محسن (Optimizer)**: Adam أو SGD

### 4. **مجموعة البيانات**

**Fashion-MNIST**: مجموعة بيانات تحتوي على 70,000 صورة رمادية بحجم 28×28 بكسل لـ 10 فئات من الملابس:
- T-shirt/top
- Trouser
- Pullover
- Dress
- Coat
- Sandal
- Shirt
- Sneaker
- Bag
- Ankle boot

البيانات مقسمة إلى:
- 60,000 صورة للتدريب (مع تقسيم فرعي للتحقق)
- 10,000 صورة للاختبار

### 5. **واجهة المستخدم الرسومية (Streamlit Dashboard)**

المشروع يحتوي على لوحة تحكم تفاعلية توفر:
- **ملخص الأداء**: جدول يوضح أفضل النتائج لكل خوارزمية
- **مخططات التقارب**: رسوم بيانية توضح تطور فقدان التحقق عبر التكرارات
- **توزيع النتائج**: مقارنة توزيع أفضل النتائج بين الخوارزميات
- **أفضل المعاملات**: عرض أفضل المعاملات الفائقة لكل خوارزمية
- **تشغيل التجارب**: إمكانية تشغيل تجارب جديدة من الواجهة

---

## 📦 التثبيت والإعداد

### المتطلبات الأساسية

- Python 3.9 أو أحدث
- CUDA (اختياري، لتسريع التدريب على GPU)

### خطوات التثبيت

1. **استنساخ المستودع**:
```bash
git clone https://github.com/A7mdSl7/DeepEvoOpt.git
cd DeepEvoOpt
```

2. **إنشاء بيئة افتراضية (موصى به)**:
```bash
python -m venv venv
# على Windows
venv\Scripts\activate
# على Linux/Mac
source venv/bin/activate
```

3. **تثبيت المتطلبات**:
```bash
pip install -r requirements.txt
```

### المتطلبات الرئيسية

- `torch` - PyTorch للتعلم العميق
- `torchvision` - لمعالجة الصور ومجموعات البيانات
- `numpy` - العمليات الحسابية
- `matplotlib` - الرسوم البيانية
- `pandas` - معالجة البيانات
- `streamlit` - واجهة المستخدم الرسومية
- `plotly` - رسوم بيانية تفاعلية
- `jupyter` - دفاتر Jupyter للتجارب

---

## 💻 الاستخدام

### 1. تشغيل التجارب عبر سطر الأوامر (CLI)

#### تشغيل خوارزمية واحدة:
```bash
# تشغيل GA على CNN
python src/run_experiments.py --optimizer ga --model_type cnn --pop_size 10 --max_iter 5

# تشغيل PSO على MLP
python src/run_experiments.py --optimizer pso --model_type mlp --pop_size 15 --max_iter 10
```

#### تشغيل جميع الخوارزميات:
```bash
python src/run_experiments.py --optimizer all --model_type cnn --pop_size 10 --max_iter 5
```

#### معاملات سطر الأوامر:
- `--optimizer`: اسم الخوارزمية (`ga`, `pso`, `gwo`, `aco`, `firefly`, `abc`, `obc_woa`, `fcr`, `fcgwo`) أو `all`
- `--model_type`: نوع النموذج (`cnn` أو `mlp`)
- `--pop_size`: حجم السكان (عدد الحلول في كل جيل)
- `--max_iter`: عدد التكرارات القصوى
- `--train_final`: تدريب النموذج النهائي بعد التحسين (اختياري)

#### مثال كامل:
```bash
python src/run_experiments.py --optimizer ga --model_type cnn --pop_size 20 --max_iter 10 --train_final
```

### 2. استخدام دفاتر Jupyter

1. افتح دفتر Jupyter:
```bash
jupyter notebook
```

2. افتح `notebooks/Experiments.ipynb`
3. قم بتشغيل الخلايا لتجربة الخوارزميات المختلفة ومقارنة النتائج

### 3. استخدام واجهة Streamlit

#### تشغيل لوحة التحكم:
```bash
streamlit run app_streamlit.py
```

ستفتح الواجهة في المتصفح على العنوان `http://localhost:8501`

#### ميزات الواجهة:
- **اختيار الخوارزميات**: حدد الخوارزميات التي تريد مقارنتها
- **اختيار نوع النموذج**: CNN أو MLP
- **تصفية البيانات**: تطبيق التمهيد (Smoothing) على البيانات
- **تحميل البيانات**: إمكانية تحميل ملفات CSV مخصصة
- **تشغيل تجارب جديدة**: تشغيل تجارب محدودة من الواجهة

### 4. تدريب النموذج النهائي

بعد العثور على أفضل المعاملات، يمكنك تدريب النموذج النهائي:

```bash
python src/train.py --config results/logs/ga_cnn_best.json --model_type cnn --epochs 20
```

---

## 📊 فهم النتائج

### ملفات النتائج

بعد تشغيل التجارب، يتم حفظ النتائج في `results/logs/`:

1. **ملفات JSON** (`*_best.json`):
   - تحتوي على أفضل المعاملات الفائقة التي تم العثور عليها
   - مثال: `ga_cnn_best.json`

2. **ملفات CSV** (`*_history.csv`):
   - تحتوي على تاريخ التطور عبر التكرارات
   - الأعمدة: `iteration`, `val_loss`
   - مثال: `ga_cnn_history.csv`

### قراءة النتائج

```python
import json
import pandas as pd

# قراءة أفضل المعاملات
with open('results/logs/ga_cnn_best.json', 'r') as f:
    best_params = json.load(f)
    print("أفضل المعاملات:", best_params)

# قراءة تاريخ التطور
history_df = pd.read_csv('results/logs/ga_cnn_history.csv')
print(history_df.head())
```

---

## 🔬 كيفية عمل النظام

### 1. دالة الهدف (Objective Function)

دالة الهدف (`src/utils/objective_fn.py`) تقوم بـ:
1. إنشاء النموذج بالمعاملات الفائقة المحددة
2. تدريب النموذج لعدد محدود من العصور (3 عصور افتراضياً)
3. تقييم النموذج على مجموعة التحقق
4. إرجاع فقدان التحقق كقيمة اللياقة

### 2. عملية التحسين

كل خوارزمية تحسين:
1. **التهيئة**: إنشاء مجموعة أولية من الحلول (المعاملات الفائقة)
2. **التقييم**: تقييم كل حل باستخدام دالة الهدف
3. **التحديث**: تحديث الحلول بناءً على قواعد الخوارزمية
4. **التكرار**: تكرار الخطوات حتى الوصول لعدد التكرارات القصوى
5. **الإرجاع**: إرجاع أفضل حل تم العثور عليه

### 3. مساحة البحث

المساحة البحثية محددة في `src/run_experiments.py`:
- المعاملات المستمرة (Continuous): نطاقات قيم
- المعاملات المنفصلة (Discrete): قوائم قيم محددة

---

## 🐳 النشر باستخدام Docker

### بناء الصورة:
```bash
docker build -t deepevoopt .
```

### تشغيل الحاوية:
```bash
docker run -p 8501:8501 deepevoopt
```

ستكون الواجهة متاحة على `http://localhost:8501`

---

## 📈 أمثلة على الاستخدام

### مثال 1: مقارنة GA و PSO على CNN

```bash
# تشغيل GA
python src/run_experiments.py --optimizer ga --model_type cnn --pop_size 15 --max_iter 10

# تشغيل PSO
python src/run_experiments.py --optimizer pso --model_type cnn --pop_size 15 --max_iter 10

# فتح Streamlit لمقارنة النتائج
streamlit run app_streamlit.py
```

### مثال 2: البحث الشامل على MLP

```bash
# تشغيل جميع الخوارزميات
python src/run_experiments.py --optimizer all --model_type mlp --pop_size 20 --max_iter 15
```

### مثال 3: تدريب النموذج النهائي

```bash
# بعد العثور على أفضل المعاملات
python src/train.py --config results/logs/pso_cnn_best.json --model_type cnn --epochs 30
```

---

## 🎯 أفضل الممارسات

1. **حجم السكان**: استخدم حجم سكان أكبر (20-50) للبحث الأفضل، لكنه أبطأ
2. **عدد التكرارات**: استخدم 10-20 تكرار للحصول على نتائج جيدة
3. **النماذج**: CNN عادة ما تعطي نتائج أفضل من MLP للصور
4. **GPU**: استخدم GPU لتسريع التدريب إذا كان متاحاً
5. **البذور**: النظام يستخدم بذرة ثابتة (42) لإعادة إنتاج النتائج

---

## 🔧 التخصيص

### تعديل مساحة البحث

عدّل `DEFAULT_SEARCH_SPACE` في `src/run_experiments.py`:

```python
DEFAULT_SEARCH_SPACE = {
    'lr': (0.0001, 0.01),
    'batch_size': [32, 64, 128, 256],  # أضف قيماً جديدة
    'dropout': (0.1, 0.5),
    # ... إلخ
}
```

### إضافة خوارزمية جديدة

1. أنشئ ملف جديد في `src/optimizers/`
2. نفذ دالة `optimize()` بنفس التوقيع
3. أضف الخوارزمية إلى `OPTIMIZERS` في `src/run_experiments.py`

---

## 📝 الملاحظات المهمة

- **الوقت**: التحسين قد يستغرق وقتاً طويلاً حسب حجم السكان وعدد التكرارات
- **الذاكرة**: تأكد من وجود ذاكرة كافية، خاصة عند استخدام GPU
- **البيانات**: البيانات يتم تحميلها تلقائياً عند أول تشغيل
- **النتائج**: النتائج تُحفظ تلقائياً في `results/logs/`

---

## 🤝 المساهمة

المشروع مفتوح للمساهمات! يمكنك:
- إضافة خوارزميات تحسين جديدة
- تحسين النماذج الحالية
- إضافة ميزات جديدة للواجهة
- تحسين الأداء والكفاءة

---

## 📄 الترخيص

هذا المشروع مخصص للأغراض التعليمية والبحثية.

---

## 👤 المؤلف

المشروع من تطوير: [A7mdSl7](https://github.com/A7mdSl7)

---

## 📚 المراجع

- [PyTorch Documentation](https://pytorch.org/docs/)
- [Streamlit Documentation](https://docs.streamlit.io/)
- [Fashion-MNIST Dataset](https://github.com/zalandoresearch/fashion-mnist)

---

## 🐛 الإبلاغ عن المشاكل

إذا واجهت أي مشاكل، يرجى فتح issue في المستودع.

---

**ملاحظة**: هذا المشروع جزء من مشروع أكاديمي لدراسة خوارزميات التحسين التطورية في سياق التعلم العميق.
